package code._4_student_effort.iterator;

public interface MyIterator {
    // method that checks if the next element exists
    boolean hasNext();

    // method that returns the item
    int next();
}
